
import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFF7FAFC);      // خلفية هادئة جداً
  static const Color curveTop1 = Color(0xFFB2DFDB);       // تركواز فاتح
  static const Color curveTop2 = Color(0xFFE0F7FA);       // تركواز أفتح
  static const Color curveBottom1 = Color(0xFFE0F2F1);    // تركواز أفتح جداً
  static const Color curveBottom2 = Color(0xFFB2DFDB);    // تركواز فاتح
  static const Color card = Colors.white;                 // خلفية البطاقات
  static const Color shadow = Colors.teal;                // ظل هادئ
  static const Color accent = Color(0xFF80CBC4);          // تركواز هادئ للأيقونات
  static const Color textMain = Color(0xFF455A64);        // رمادي أزرق هادئ للنصوص
  static const Color sliderActive = Colors.teal;          // لون السلايدر النشط
}